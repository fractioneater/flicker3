# Let's break everything!
#  - Claude, 2026

val x = 0b2222
val y = 0xZZZ

"unclosed string with bad escape \q and interpolation =(

'this is a long character literal oops'

''

`

`also unclosed

#- comment #- nested #- nested #- nested #- nested #- nested #- nested #- nested #- nested #- nested #- nested #- nested #- nested #- nested #- nested #- nested #- nested #- way too deep -# -# -# -# -# -# -# -# -# -# -# -# -# -# -# -# -#

each i in 1..5
    print(i)
        print("wrong indent level")
    print("back to normal?")
  print("misaligned dedent")

val emoji = "hello \UFFFFFFFF world"

val char = '\q'

val number = 123abc

` `

@$%

val text = "=(1 + =(2 + =(3 + =(4 + =(5 + =(6 + =(7 + =(8 + =(9)))))))))nested interpolation madness"

#- this block comment never ends
